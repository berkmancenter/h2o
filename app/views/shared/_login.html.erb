<% if !current_user || @page_cache -%>
<div id="login-popup" class="standard-form popup">
   <p>H2O will be switching to read-only mode next Wednesday, June 20th; at that time you will no longer be able to log in and make changes. We are working to ensure data is migrated appropriately; please contact Brett at <a href="mailto:h2o@cyber.law.harvard.edu">h2o@cyber.law.harvard.edu</a> with any questions.</p>
  <div class="right">
    <h3>Returning user log in</h3>
    <%= form_for UserSession.new do |f| %>
    <%= f.label :login %>
    <%= f.text_field :login, :class => 'text' %>
    <%= f.label :password %>
    <%= f.password_field :password, :class => 'text' %>
    <div class="remember">
    <%= link_to 'Forgot your password?', new_password_reset_path %>
    </div>
    <div class="remember">
    <%= f.check_box :remember_me %><%= f.label :remember_me %>
    </div>
    <div class="center">
    <%= f.submit "LOGIN", :class => 'button' %>
    </div>
    <% end -%>
  </div>
  <div class="left">
    <h3>Are you a first-time user?</h3>
    <a href="/users/new/" class="button">SIGN UP NOW</a>
  </div>
</div>
<% end -%>
